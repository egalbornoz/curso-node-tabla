# Notas
Este es mi primer prorama en Node.js
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Especifica el numero base con el que se genera la tabla
                                                             [number] [required]
  -l, --listar   Se usa para listar la tabla creada   [boolean] [default: false]
  -h, --hasta    Se usa para indicar hasta donde llega la multiplicacion
                                                          [number] [default: 10]
```